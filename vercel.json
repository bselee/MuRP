{
  "buildCommand": "npm run build",
  "functions": {
    "api/**/*.ts": {
      "includeFiles": "lib/**/*"
    }
  },
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
