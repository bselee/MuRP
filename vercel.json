{
  "buildCommand": "npm run build",
  "functions": {
    "api/**/*.ts": {
      "includeFiles": "lib/**/*"
    }
  },
  "crons": [
    {
      "path": "/api/sync-finale",
      "schedule": "0 */4 * * *"
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
